"use client"

// PhotonCore is now rendered entirely via canvas in HeroScene.tsx
// This file is kept for future use / standalone photon exports.

import React from 'react';

export default function PhotonCore() {
  return null;
}
